import { isfunc } from 'piral-base';
export var defaultLayouts = ['desktop', 'tablet', 'mobile'];
export var defaultBreakpoints = ['(min-width: 991px)', '(min-width: 481px)', '(max-width: 480px)'];
var mm = typeof window === 'undefined' || !isfunc(window.matchMedia)
    ? function () { return ({ matches: [] }); }
    : function (q) { return window.matchMedia(q); };
export function getCurrentLayout(breakpoints, layouts, defaultLayout) {
    var query = breakpoints.findIndex(function (q) { return mm(q).matches; });
    var layout = layouts[query];
    return layout !== undefined ? layout : defaultLayout;
}
//# sourceMappingURL=media.js.map