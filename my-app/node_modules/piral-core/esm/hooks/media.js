import { useState, useEffect } from 'react';
import { getCurrentLayout } from '../utils/media';
/**
 * Hook to detect layout changes (e.g., which UI breakpoint was hit).
 * @param queries The available queries matching the breakpoints.
 * @param values The values mapping to the breakpoints
 * @param defaultValue The default value.
 */
export function useMedia(queries, values, defaultValue) {
    var match = function () { return getCurrentLayout(queries, values, defaultValue); };
    var _a = useState(match), value = _a[0], update = _a[1];
    useEffect(function () {
        if (typeof document !== 'undefined') {
            var handler_1 = function () { return update(match); };
            window.addEventListener('resize', handler_1);
            return function () { return window.removeEventListener('resize', handler_1); };
        }
    }, []);
    return value;
}
//# sourceMappingURL=media.js.map