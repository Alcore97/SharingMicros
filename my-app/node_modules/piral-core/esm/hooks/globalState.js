import { useContext } from 'react';
import { useAtom, deref } from '@dbeining/react-atom';
import { StateContext } from '../state/stateContext';
var useGlobalAtom = typeof window !== 'undefined' ? useAtom : useDirectAtom;
function useDirectAtom(atom, opts) {
    var state = deref(atom);
    var select = opts && opts.select;
    return typeof select === 'function' ? select(state) : state;
}
export function useGlobalState(select) {
    var state = useContext(StateContext).state;
    return useGlobalAtom(state, select && { select: select });
}
//# sourceMappingURL=globalState.js.map