"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAppInstance = void 0;
var api_1 = require("./api");
var utils_1 = require("./utils");
/**
 * Gets a new instantiated Piral instance using the provided options.
 * Very useful for server-side rendering.
 * @param config The configuration for creating the Piral instance.
 * @param options The options to use when rendering the Piral instance.
 * @example
```ts
import { render } from 'react-dom';
import { getAppInstance } from 'piral';

const { app } = getAppInstance();
render(app, document.querySelector('#app'));
```
 */
function getAppInstance(config, options) {
    if (config === void 0) { config = {}; }
    if (options === void 0) { options = {}; }
    var settings = options.settings, layout = options.layout, errors = options.errors, dashboardPath = options.dashboardPath, piralChildren = options.piralChildren;
    var instance = api_1.createPiral(config, settings);
    var app = utils_1.createInstanceElement(instance, layout, errors, dashboardPath, piralChildren);
    return { instance: instance, app: app };
}
exports.getAppInstance = getAppInstance;
//# sourceMappingURL=app.js.map