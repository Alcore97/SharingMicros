import { useEffect } from 'react';
import { useGlobalState, useAction } from 'piral-core';
/**
 * Hook that returns the connected feed.
 * @param options The options for retrieving the feed.
 */
export function useFeed(options) {
    var _a = useGlobalState(function (s) { return s.feeds[options.id]; }), loaded = _a.loaded, loading = _a.loading, error = _a.error, data = _a.data;
    var load = useAction('loadFeed');
    useEffect(function () {
        if (!loaded && !loading) {
            load(options);
        }
    }, [loaded]);
    return [loaded, data, error];
}
//# sourceMappingURL=useFeed.js.map