import { __assign as __assign_1, __spreadArray } from "tslib";
import { __assign } from 'tslib';
export function initializeApi(target, events) {
    return {
        on: events.on,
        off: events.off,
        emit: events.emit,
        meta: __assign_1({}, target),
    };
}
export function mergeApis(api, extenders, target) {
    var frags = extenders.map(function (extender) { return extender(api, target); });
    __assign.apply(void 0, __spreadArray([api], frags));
    return api;
}
//# sourceMappingURL=api.js.map